apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myRedisChart.fullname" . }}-test-connection"
  labels:
    {{- include "myRedisChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myRedisChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
